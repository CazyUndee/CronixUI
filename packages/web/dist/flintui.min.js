(function(global) {
'use strict';
function $(selector, context = document) {
return context.querySelector(selector);
}
function $$(selector, context = document) {
return Array.from(context.querySelectorAll(selector));
}
function createEl(tag, className, attrs = {}) {
const el = document.createElement(tag);
if (className) el.className = className;
Object.entries(attrs).forEach(([key, val]) => {
if (key === 'text') el.textContent = val;
else if (key === 'html') el.innerHTML = val;
else el.setAttribute(key, val);
});
return el;
}
class FlintToggle {
constructor(el) {
this.el = el;
this.input = el.querySelector('input');
this.box = el.querySelector('.fl-toggle-box');
if (this.el.tagName === 'DIV' && !this.input) {
el.addEventListener('click', () => this.toggle());
}
el._flToggle = this;
}
toggle() {
this.el.classList.toggle('on');
this.el.dispatchEvent(new CustomEvent('change', {
detail: { value: this.isOn() }
}));
}
isOn() {
return this.el.classList.contains('on');
}
setOn(on) {
this.el.classList.toggle('on', on);
}
}
function initToggles() {
$$('.fl-toggle').forEach(el => {
if (!el._flToggle) new FlintToggle(el);
});
}
class FlintNav {
constructor(el) {
this.el = el;
this.items = $$('.fl-nav-item', el);
this.items.forEach(item => {
item.addEventListener('click', () => this.setActive(item));
});
el._flNav = this;
}
setActive(activeItem) {
this.items.forEach(item => item.classList.remove('fl-nav-active'));
activeItem.classList.add('fl-nav-active');
this.el.dispatchEvent(new CustomEvent('change', {
detail: { item: activeItem }
}));
}
}
function initNavs() {
$$('.fl-nav').forEach(el => {
if (!el._flNav) new FlintNav(el);
});
}
class FlintTabs {
constructor(el) {
this.el = el;
this.tabs = $$('.fl-tab', el);
this.panels = $$('.fl-tab-panel', el.parentElement);
this.tabs.forEach((tab, i) => {
tab.addEventListener('click', () => this.setActive(i));
});
el._flTabs = this;
}
setActive(index) {
this.tabs.forEach((tab, i) => {
tab.classList.toggle('fl-tab-active', i === index);
});
this.panels.forEach((panel, i) => {
panel.classList.toggle('fl-tab-panel-active', i === index);
});
this.el.dispatchEvent(new CustomEvent('change', {
detail: { index }
}));
}
}
function initTabs() {
$$('.fl-tabs').forEach(el => {
if (!el._flTabs) new FlintTabs(el);
});
}
class FlintAccordion {
constructor(el) {
this.el = el;
this.items = $$('.fl-accordion-item', el);
this.multi = el.dataset.multi === 'true';
this.items.forEach(item => {
const header = $('.fl-accordion-header', item);
header.addEventListener('click', () => this.toggle(item));
});
el._flAccordion = this;
}
toggle(item) {
const isOpen = item.classList.contains('fl-accordion-open');
if (!this.multi) {
this.items.forEach(i => i.classList.remove('fl-accordion-open'));
}
item.classList.toggle('fl-accordion-open', !isOpen);
}
openAll() {
this.items.forEach(item => item.classList.add('fl-accordion-open'));
}
closeAll() {
this.items.forEach(item => item.classList.remove('fl-accordion-open'));
}
}
function initAccordions() {
$$('.fl-accordion').forEach(el => {
if (!el._flAccordion) new FlintAccordion(el);
});
}
class FlintModal {
constructor(el) {
this.el = el;
this.modal = $('.fl-modal', el);
this.closeBtn = $('.fl-modal-close', el);
this.onKeydown = this.onKeydown.bind(this);
if (this.closeBtn) {
this.closeBtn.addEventListener('click', () => this.close());
}
el.addEventListener('click', (e) => {
if (e.target === el) this.close();
});
el._flModal = this;
}
open() {
this.el.classList.add('fl-modal-open');
document.body.style.overflow = 'hidden';
document.addEventListener('keydown', this.onKeydown);
this.el.dispatchEvent(new CustomEvent('open'));
}
close() {
this.el.classList.remove('fl-modal-open');
document.body.style.overflow = '';
document.removeEventListener('keydown', this.onKeydown);
this.el.dispatchEvent(new CustomEvent('close'));
}
onKeydown(e) {
if (e.key === 'Escape') this.close();
}
}
function initModals() {
$$('.fl-modal-backdrop').forEach(el => {
if (!el._flModal) new FlintModal(el);
});
}
class FlintDropdown {
constructor(el) {
this.el = el;
this.trigger = $('.fl-dropdown-trigger', el) || el.querySelector('[data-dropdown-trigger]') || el;
this.menu = $('.fl-dropdown-menu', el);
this.onDocClick = this.onDocClick.bind(this);
this.trigger.addEventListener('click', (e) => {
e.stopPropagation();
this.toggle();
});
this.menu.addEventListener('click', () => this.close());
el._flDropdown = this;
}
open() {
this.el.classList.add('fl-dropdown-open');
document.addEventListener('click', this.onDocClick);
}
close() {
this.el.classList.remove('fl-dropdown-open');
document.removeEventListener('click', this.onDocClick);
}
toggle() {
if (this.el.classList.contains('fl-dropdown-open')) {
this.close();
} else {
this.open();
}
}
onDocClick(e) {
if (!this.el.contains(e.target)) {
this.close();
}
}
}
function initDropdowns() {
$$('.fl-dropdown').forEach(el => {
if (!el._flDropdown) new FlintDropdown(el);
});
}
class FlintToast {
constructor(container) {
this.container = container || this.getOrCreateContainer();
}
getOrCreateContainer() {
let container = $('.fl-toast-container');
if (!container) {
container = createEl('div', 'fl-toast-container');
document.body.appendChild(container);
}
return container;
}
show(options) {
const {
title,
message,
type = 'info',
duration = 4000,
closable = true
} = options;
const toast = createEl('div', `fl-toast fl-toast-${type}`);
toast.innerHTML = `
<div class="fl-alert-icon">
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
${this.getIcon(type)}
</svg>
</div>
<div class="fl-alert-content">
${title ? `<div class="fl-alert-title">${title}</div>` : ''}
${message ? `<div class="fl-alert-message">${message}</div>` : ''}
</div>
${closable ? `
<button class="fl-alert-close">
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
<line x1="18" y1="6" x2="6" y2="18"></line>
<line x1="6" y1="6" x2="18" y2="18"></line>
</svg>
</button>
` : ''}
`;
if (closable) {
$('.fl-alert-close', toast).addEventListener('click', () => this.hide(toast));
}
this.container.appendChild(toast);
if (duration > 0) {
setTimeout(() => this.hide(toast), duration);
}
return toast;
}
hide(toast) {
toast.classList.add('fl-toast-leaving');
setTimeout(() => toast.remove(), 200);
}
getIcon(type) {
const icons = {
success: '<polyline points="20 6 9 17 4 12"></polyline>',
error: '<circle cx="12" cy="12" r="10"></circle><line x1="15" y1="9" x2="9" y2="15"></line><line x1="9" y1="9" x2="15" y2="15"></line>',
warning: '<path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path><line x1="12" y1="9" x2="12" y2="13"></line><line x1="12" y1="17" x2="12.01" y2="17"></line>',
info: '<circle cx="12" cy="12" r="10"></circle><line x1="12" y1="16" x2="12" y2="12"></line><line x1="12" y1="8" x2="12.01" y2="8"></line>'
};
return icons[type] || icons.info;
}
success(message, title) { return this.show({ message, title, type: 'success' }); }
error(message, title) { return this.show({ message, title, type: 'error' }); }
warning(message, title) { return this.show({ message, title, type: 'warning' }); }
info(message, title) { return this.show({ message, title, type: 'info' }); }
}
class FlintCommandPalette {
constructor(el) {
this.el = el;
this.input = $('.fl-command-palette-input', el);
this.results = $('.fl-command-palette-results', el);
this.items = [];
this.activeIndex = -1;
this.onKeydown = this.onKeydown.bind(this);
this.input.addEventListener('input', () => this.filter());
this.input.addEventListener('keydown', (e) => this.onKeydown(e));
el.addEventListener('click', (e) => {
if (e.target === el) this.close();
});
el._flCommandPalette = this;
}
setItems(items) {
this.items = items;
this.render();
}
render() {
const query = this.input.value.toLowerCase();
const filtered = this.items.filter(item =>
item.title.toLowerCase().includes(query) ||
(item.subtitle && item.subtitle.toLowerCase().includes(query))
);
this.results.innerHTML = filtered.map((item, i) => `
<div class="fl-command-item${i === 0 ? ' fl-command-item-active' : ''}" data-index="${i}">
${item.icon ? `<div class="fl-command-item-icon">${item.icon}</div>` : ''}
<div class="fl-command-item-content">
<div class="fl-command-item-title">${item.title}</div>
${item.subtitle ? `<div class="fl-command-item-subtitle">${item.subtitle}</div>` : ''}
</div>
${item.kbd ? `<div class="fl-command-item-kbd">${item.kbd}</div>` : ''}
</div>
`).join('');
$$('.fl-command-item', this.results).forEach((item, i) => {
item.addEventListener('click', () => this.select(filtered[i]));
item.addEventListener('mouseenter', () => this.setActive(i));
});
this.activeIndex = filtered.length > 0 ? 0 : -1;
}
filter() {
this.render();
}
onKeydown(e) {
const items = $$('.fl-command-item', this.results);
if (e.key === 'ArrowDown') {
e.preventDefault();
this.setActive(Math.min(this.activeIndex + 1, items.length - 1));
} else if (e.key === 'ArrowUp') {
e.preventDefault();
this.setActive(Math.max(this.activeIndex - 1, 0));
} else if (e.key === 'Enter' && this.activeIndex >= 0) {
e.preventDefault();
items[this.activeIndex].click();
}
}
setActive(index) {
const items = $$('.fl-command-item', this.results);
items.forEach((item, i) => item.classList.toggle('fl-command-item-active', i === index));
this.activeIndex = index;
}
select(item) {
if (item.action) item.action();
this.close();
this.el.dispatchEvent(new CustomEvent('select', { detail: item }));
}
open() {
this.el.classList.add('fl-command-palette-open');
this.input.value = '';
this.render();
setTimeout(() => this.input.focus(), 100);
document.body.style.overflow = 'hidden';
document.addEventListener('keydown', this.handleEscape);
}
close() {
this.el.classList.remove('fl-command-palette-open');
document.body.style.overflow = '';
document.removeEventListener('keydown', this.handleEscape);
}
handleEscape = (e) => {
if (e.key === 'Escape') this.close();
};
}
function initCommandPalettes() {
$$('.fl-command-palette').forEach(el => {
if (!el._flCommandPalette) new FlintCommandPalette(el);
});
}
class FlintSearch {
constructor(el) {
this.el = el;
this.input = $('.fl-search-input', el);
this.results = $('.fl-search-results', el);
this.onDocClick = this.onDocClick.bind(this);
this.input.addEventListener('focus', () => this.open());
this.input.addEventListener('input', () => this.filter());
el.addEventListener('click', (e) => e.stopPropagation());
el._flSearch = this;
}
setItems(items) {
this.items = items;
}
filter() {
const query = this.input.value.toLowerCase();
if (!query) {
this.close();
return;
}
const filtered = this.items.filter(item =>
item.title.toLowerCase().includes(query) ||
(item.subtitle && item.subtitle.toLowerCase().includes(query))
);
this.results.innerHTML = filtered.map(item => `
<div class="fl-search-result">
<div class="fl-search-result-title">${item.title}</div>
${item.subtitle ? `<div class="fl-search-result-subtitle">${item.subtitle}</div>` : ''}
</div>
`).join('');
$$('.fl-search-result', this.results).forEach((result, i) => {
result.addEventListener('click', () => {
this.select(filtered[i]);
});
});
this.open();
}
select(item) {
this.input.value = item.title;
this.close();
this.el.dispatchEvent(new CustomEvent('select', { detail: item }));
if (item.action) item.action();
}
open() {
this.el.classList.add('fl-search-open');
document.addEventListener('click', this.onDocClick);
}
close() {
this.el.classList.remove('fl-search-open');
document.removeEventListener('click', this.onDocClick);
}
onDocClick(e) {
if (!this.el.contains(e.target)) {
this.close();
}
}
}
function initSearch() {
$$('.fl-search').forEach(el => {
if (!el._flSearch) new FlintSearch(el);
});
}
class FlintAlert {
constructor(el) {
this.el = el;
this.closeBtn = $('.fl-alert-close', el);
if (this.closeBtn) {
this.closeBtn.addEventListener('click', () => this.close());
}
el._flAlert = this;
}
close() {
this.el.style.opacity = '0';
setTimeout(() => this.el.remove(), 200);
}
}
function initAlerts() {
$$('.fl-alert').forEach(el => {
if (!el._flAlert) new FlintAlert(el);
});
}
class FlintTable {
constructor(el) {
this.el = el;
this.table = $('table', el) || el;
this.headers = $$('th[data-sort]', this.table);
this.headers.forEach(header => {
header.addEventListener('click', () => this.sort(header));
});
el._flTable = this;
}
sort(header) {
const key = header.dataset.sort;
const tbody = $('tbody', this.table);
const rows = $$('tr', tbody);
const order = header.dataset.order === 'asc' ? 'desc' : 'asc';
this.headers.forEach(h => delete h.dataset.order);
header.dataset.order = order;
const index = Array.from(header.parentElement.children).indexOf(header);
rows.sort((a, b) => {
const aVal = a.children[index].textContent.trim();
const bVal = b.children[index].textContent.trim();
const aNum = parseFloat(aVal);
const bNum = parseFloat(bVal);
if (!isNaN(aNum) && !isNaN(bNum)) {
return order === 'asc' ? aNum - bNum : bNum - aNum;
}
return order === 'asc'
? aVal.localeCompare(bVal)
: bVal.localeCompare(aVal);
});
rows.forEach(row => tbody.appendChild(row));
}
}
function initTables() {
$$('.fl-table-sortable').forEach(el => {
if (!el._flTable) new FlintTable(el);
});
}
class FlintPagination {
constructor(el, options = {}) {
this.el = el;
this.total = options.total || 1;
this.current = options.current || 1;
this.onChange = options.onChange || (() => {});
this.render();
el._flPagination = this;
}
render() {
const pages = this.getPages();
this.el.innerHTML = `
<button class="fl-pagination-item" data-page="prev" ${this.current === 1 ? 'disabled' : ''}>
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
<polyline points="15 18 9 12 15 6"></polyline>
</svg>
</button>
${pages.map(p => `
<button class="fl-pagination-item${p === this.current ? ' fl-pagination-active' : ''}" data-page="${p}">
${p}
</button>
`).join('')}
<button class="fl-pagination-item" data-page="next" ${this.current === this.total ? 'disabled' : ''}>
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
<polyline points="9 18 15 12 9 6"></polyline>
</svg>
</button>
`;
$$('.fl-pagination-item', this.el).forEach(btn => {
btn.addEventListener('click', () => {
const page = btn.dataset.page;
if (page === 'prev' && this.current > 1) {
this.goTo(this.current - 1);
} else if (page === 'next' && this.current < this.total) {
this.goTo(this.current + 1);
} else if (!isNaN(page)) {
this.goTo(parseInt(page));
}
});
});
}
getPages() {
const pages = [];
const maxVisible = 5;
if (this.total <= maxVisible) {
for (let i = 1; i <= this.total; i++) pages.push(i);
} else {
if (this.current <= 3) {
for (let i = 1; i <= 4; i++) pages.push(i);
pages.push('...');
pages.push(this.total);
} else if (this.current >= this.total - 2) {
pages.push(1);
pages.push('...');
for (let i = this.total - 3; i <= this.total; i++) pages.push(i);
} else {
pages.push(1);
pages.push('...');
for (let i = this.current - 1; i <= this.current + 1; i++) pages.push(i);
pages.push('...');
pages.push(this.total);
}
}
return pages;
}
goTo(page) {
if (page >= 1 && page <= this.total && page !== this.current) {
this.current = page;
this.render();
this.onChange(page);
this.el.dispatchEvent(new CustomEvent('change', { detail: { page } }));
}
}
}
class FlintFileInput {
constructor(el) {
this.el = el;
this.input = $('input[type="file"]', el);
this.label = $('.fl-file-input-label', el);
this.textEl = $('.fl-file-input-text', el);
this.originalText = this.textEl ? this.textEl.innerHTML : '';
this.input.addEventListener('change', (e) => this.handleChange(e));
el._flFileInput = this;
}
handleChange(e) {
const files = this.input.files;
if (files.length > 0) {
const names = Array.from(files).map(f => f.name).join(', ');
if (this.textEl) {
this.textEl.innerHTML = `<span>${names}</span>`;
}
this.el.classList.add('fl-file-input-has-file');
this.el.dispatchEvent(new CustomEvent('change', { detail: { files } }));
} else {
this.reset();
}
}
reset() {
this.input.value = '';
if (this.textEl) this.textEl.innerHTML = this.originalText;
this.el.classList.remove('fl-file-input-has-file');
}
}
function initFileInputs() {
$$('.fl-file-input').forEach(el => {
if (!el._flFileInput) new FlintFileInput(el);
});
}
function init() {
initToggles();
initNavs();
initTabs();
initAccordions();
initModals();
initDropdowns();
initCommandPalettes();
initSearch();
initAlerts();
initTables();
initFileInputs();
}
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', init);
} else {
init();
}
const FlintUI = {
init,
Toggle: FlintToggle,
Nav: FlintNav,
Tabs: FlintTabs,
Accordion: FlintAccordion,
Modal: FlintModal,
Dropdown: FlintDropdown,
Toast: new FlintToast(),
CommandPalette: FlintCommandPalette,
Search: FlintSearch,
Alert: FlintAlert,
Table: FlintTable,
Pagination: FlintPagination,
FileInput: FlintFileInput,
$,
$$,
createEl
};
if (typeof define === 'function' && define.amd) {
define(() => FlintUI);
}
else if (typeof module !== 'undefined' && module.exports) {
module.exports = FlintUI;
}
else {
global.FlintUI = FlintUI;
}
})(typeof globalThis !== 'undefined' ? globalThis : window);